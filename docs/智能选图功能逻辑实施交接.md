# 智能选图功能逻辑实施交接

> 版本：1.2
> 日期：2026-08-20
> 当前阶段：五个可选模式已拆为七个固定权重解析策略；眼态契约已冻结，新光学/构图模型仍受外部 Gate 限制
> 用途：新窗口直接据此继续实现，不重复讨论，不把旧代码行为误当成最新需求  
> 强制约束：同时遵守根目录 AGENTS.md 与 docs/feature-integration-guidelines.md

## 0. 阅读顺序与文档效力

新窗口开始工作前，依次阅读：

1. 根目录 AGENTS.md；
2. docs/feature-integration-guidelines.md；
3. 本文；
4. docs/智能选图功能需求基线与待调研清单.md，仅用于了解历史讨论和详细调研证据；
5. 本次实现涉及的代码、调用方、导出和测试。

发生冲突时，采用以下优先级：

1. 用户在新窗口中的最新明确要求；
2. AGENTS.md 和二开接入规范中的安全、隔离要求；
3. 本文；
4. 旧需求基线和其他历史文档；
5. 当前尚未按新需求修改的代码行为。

本文状态词：

- 已确认：用户已明确确认，可作为实现约束；
- 当前方案：已形成推荐方案，但仍须经过样片、授权或双平台验证；
- 未实现：当前生产代码尚不具备该行为；
- 已验证 UI：用户确认页面结果可放行，不代表算法逻辑已完成；
- 候选模型：只允许先做隔离技术验证，不代表已批准新增依赖或生产打包。

## 1. 当前进度结论

### 1.1 已完成并获得确认

智能选图配置页中部 UI 已完成本轮改造，并由用户确认可放行：

- 中部采用弹性布局，模式文字完整显示；
- 删除任务摘要；
- 删除模型、运行时和设备等普通用户无用的技术信息；
- 简化关键人物提示；
- 删除“已选择人物”独立区域；
- 支持再次点击已选人脸取消选择；
- 补充关键人物照片条的可见缩略图请求；
- 左侧来源栏和右侧调整栏未修改。

当前 UI 相关暂存文件：

- src/features/smart-culling/SmartCullingReviewPage.tsx
- src/features/smart-culling/components/KeyPeoplePicker.tsx
- src/features/smart-culling/components/SetupScreen.tsx
- src/features/smart-culling/i18n.ts
- src/features/smart-culling/styles/setup-decision-queue.css

用户验证的是当前页面结果。关键人物预览黑屏缺少自动化桌面回归和完整根因证明，后续只补必要回归，
不得借实现功能逻辑重新设计已放行页面。

### 1.2 已完成的需求收敛与研究

已经达成一致：

- 用户可选模式从十种收敛为五种；
- 最终评分解析为七个固定权重策略；
- 明确关键人物适用范围和 A/B/C 多人物规则；
- 明确星级表示当前模式下照片的绝对质量；
- 明确相似组只负责组内排序与首选，不改变星级；
- 明确人工操作锁定、持续解锁及 RAW/JPEG 资产级保护；
- 明确疑似关键人物进入独立人工审核，系统保持 0 星；
- 明确当前文件夹同层处理且禁止递归；
- 已形成自动路由、人物识别、表情、相似组、风光质量和跨平台模型的研究方案。

### 1.3 已完成的 P0 功能逻辑

2026-08-03 已完成不新增模型和依赖的 P0 实现：

- 前后端统一收敛为自动、风光、人像、环境人像、群像五种模式；
- 指定关键人物的适用范围已修正为仅人像和群像；自动、风光、环境人像后端均拒绝非法组合；
- 文件扫描只处理当前文件夹直接包含的图片，明确忽略子文件夹；
- 在既有 metadata 来源判断上增加显式锁定与持续解锁；
- 建立任务时排除受保护资产，确认写入前复检文件、全部成员 sidecar 与人工保护状态；
- RAW/JPEG 确认配对后默认展示 JPEG，并以资产为单位同步星级与锁定数据；
- 用户修改任一 RAW/JPEG 成员后，人工结果会在安全复检后同步到整个资产并立即锁定；
- RAW/JPEG 已有结果冲突、来源未知或记录损坏时保留现状并明确报错；
- 星级只由绝对质量分映射，相似组排名、推荐数量和首选状态不再改变星级；
- 新增独立人工审核状态字段，与普通未评分语义分离；实际产生人工审核项属于 P1；
- 专项 Rust 测试 87 项通过，相关前端 ESLint、Rust 检查和格式检查通过。

### 1.4 2026-08-03 尚未完成（历史状态，以 1.7 为准）

P1 及后续准确性能力仍未实现或尚未达到生产放行标准：

- 自动模式仍只用人脸数量做粗略路由，缺少可靠人物主题和灰区判断；
- 环境人像人物主题门槛与风光回退未完成；
- 指定关键人物尚未真正控制候选、低星和人工审核；
- 表情状态仍为占位结果；
- 相似组仍主要依赖时间、序号和感知哈希；
- 风光构图及高可靠技术质量能力未完成；
- RAW/JPEG 跨两个 sidecar 的掉电及第二次写入失败恢复尚未完成桌面故障注入验证；
- 候选模型未通过授权、ONNX、Core ML、DirectML 和真实照片验证；
- 冻结验证集、星级校准和发布门槛尚未建立。

其中自动模式的横竖方向猜测已从 P0 代码移除，但可靠人物主题、灰区双路评分及环境人像回退仍须在
P1 完成，因此自动路由整体仍不能视为放行。

### 1.5 当前工作区基线

1.0 文档编写时的基线为 ae8857f0。P0 实施时工作区已前进至 f8b0207f，P0 修改尚未提交、未推送、
未发布。后续窗口仍须先做只读 Git 状态检查，不得重置、覆盖或取消用户已有暂存内容。

历史基线：

- 分支：feat-ai-chat；
- 提交：ae8857f0；
- 上述 UI 修改和旧需求基线已处于暂存状态；
- 本次只新增交接文档，并在旧基线顶部增加入口；
- 未提交、未推送、未发布。

P0 未新增依赖，未修改智能选图独立目录之外的业务文件。

### 1.6 2026-08-18 校准版本与盲测状态

- macOS Debug 校准策略已从 `3.2` 修正为 `3.3`。原因是 `v003` 落盘后评分规则又发生变化，继续沿用 `3.2` 会造成同版本不可复现；Release 策略仍保持 `3.1`。
- 既有 200 张样片及 `ai-run-v001`～`v003` 已参与阈值分析，只能用于训练与回归，不能作为独立泛化证据。
- 测试专用评测器新增严格两阶段入口：先在人工目录不可见时冻结 AI 快照，再以记录的 SHA-256 揭盲；任何图片、AI 侧车、模型或策略版本变化都会中止评测。
- 盲测实现完全位于 `src-tauri/src/features/smart_culling/`，未增加依赖，未修改上游业务文件。完整操作见 `docs/智能选图盲测流程.md`。
- 在新的独立任务集完成前，当前提升只能诚实表述为“既有校准集上的改善”，不能声称泛化可信度达到 95%。

### 1.7 2026-08-20 七策略与能力边界

- macOS Debug 策略版本冻结为 `qraw-smart-culling-policy-4.0-macos-calibration`；Release/其他构建冻结为 `qraw-smart-culling-policy-4.0-safe-evidence`。
- 眼态核心单独版本化为 `qraw-eye-model-contract-1.0` + `qraw-eye-policy-1.0`。任何表情、光学或构图改动都不得调整其阈值、替换输入或改写结论。
- 表情描述只读非眼 Blendshape，明确排除 `eye*`；当前只对同组、身份已 confirmed 的指定人物判断稳定/过渡瞬间，不评价情绪、吸引力或是否微笑。无指定人物在可靠主体跟踪完成前保持 `unknown`。
- Debug 可使用隔离的 FaceMesh/Blendshape POC 证据；Release/非 macOS Debug 明确保持眼态和表情 unavailable。两者的盲测结果不得混用。
- ARNIQA 仅完成隔离工程 POC，且 QRaw 内置 ONNX Runtime 1.22 的 Core ML 严格无 CPU 回退门禁未通过；构图能力仍无生产模型。权重授权、QRaw macOS 严格运行时、Windows DirectML、真实盲测、性能和摄影师验收未全部通过前，均不得进入发布打包或冒充生产评分。

## 2. 已确认且不可擅自改变的规则

### 2.1 上游隔离与页面边界

- 上游为 CyberTimon/RapidRAW；
- 智能选图是 QRaw 独立二开 feature；
- 前端业务主体只能位于 src/features/smart-culling/；
- Rust 业务主体只能位于 src-tauri/src/features/smart_culling/；
- 上游文件只允许统一挂载、注册、通用插槽、通用契约和必要透传；
- 禁止把评分、人物识别、锁定、分组或写入逻辑散落到上游组件和公共工具；
- 后续同步上游时保持上游提交原貌，由独立 feature 适配接口变化；
- 页面只维护中部，左侧来源栏和右侧调整栏不可修改。

### 2.2 文件夹范围

- 用户需要选中并展开当前文件夹；
- 处理该文件夹直接包含的全部支持图片；
- 不进入子文件夹，不递归；
- 子文件夹应被明确忽略，不作为失败项；
- 受保护资产即使位于当前层级，也不得被系统修改。

### 2.3 模式范围

每次只能选择一种模式，只保留：

1. 自动；
2. 风光；
3. 人像；
4. 环境人像；
5. 群像。

删除街拍、动物、建筑、产品、银河。删除必须覆盖前后端常量、类型、白名单、评分分支、文案、
原因码和测试，不能只在页面隐藏。

指定关键人物只在人像、群像显示。自动、风光和环境人像完全隐藏，后端也必须拒绝非法的
“模式 + 关键人物”组合。

### 2.4 绝对星级与相似组

- 每张照片在当前模式下独立计算绝对质量；
- 1 至 5 星只由绝对质量映射；
- 相似组内仍按每张照片的绝对质量排序；
- 组内排名和“组内首选”是独立信息，不能加星、降星或改变星级；
- 两张同组照片都足够优秀时，可以同时获得相同高星；
- 照片换文件夹或换分组，星级不能因此改变；
- 0 星表示尚未形成可采用的系统星级，或需要人工审核。

实现时必须分离三种语义：

1. 绝对质量分；
2. 仅由绝对质量映射的星级；
3. 相似组排名与首选。

当前 scoring.rs 的 rating_for 同时读取分数、排名和推荐数量，与该规则冲突。

### 2.5 人工锁定是最高安全底线

- 只有用户操作可以产生锁定，系统永远不能自动锁定；
- 用户设置星级、修改 AI 星级或手动清零到 0 星后，都立即锁定；
- AI 首次写入且用户未修改的结果不锁定；
- 用户可主动解锁，解锁持续生效；
- 解锁后系统再次写入也不能恢复锁定；
- 只有用户再次操作才重新锁定；
- RAW/JPEG 任一成员的人工操作锁定整个资产；
- 人工审核中的手动打星也锁定整个资产。

必须双重保护：

1. 建立任务清单时读取锁定和来源状态，排除受保护资产；
2. 真正写入前重新读取元数据，检查锁定、来源、版本和并发人工修改。

读取失败、来源不明、成员状态冲突或版本过期时，必须保留现状并明确报告，不能猜测后覆盖。

现有 domain/metadata.rs 已具备人工来源推断、用户修改 AI 结果、手动清零和 RAW/JPEG 资产保护基础，
应在其上扩展，不能另建第二套来源判断。

### 2.6 RAW/JPEG

- 同名且确认配对的 RAW/JPEG 显示为一个资产组；
- 默认显示 JPEG，没有 JPEG 时再显示 RAW；
- 星级、锁定、人工审核和写入保护属于整个资产组；
- 不允许同一资产出现互相矛盾的用户星级；
- 配对不明确时不得猜测。

展示成员与分析成员应在契约中分开表达。技术质量是否优先分析 RAW 标准化渲染属于当前研究建议，
尚需成对样片验证，不阻塞默认展示 JPEG 和资产级保护。

### 2.7 指定关键人物

- 可选择一位或多位，用人物 A、B、C 区分；
- A/B/C 只区分身份，不自动表示优先级；
- 一张脸不能同时满足多个指定人物；
- 照片包含全部指定人物后，才进入当前人像类模式的二次评分；
- 明确缺少任意一位时进入低星范围；
- 疑似匹配、身份歧义或不可可靠识别时进入独立人工审核；
- 人工审核提示“疑是关键人物”，系统保持 0 星，由用户手动打星；
- 用户手动确认结果不会自动扩充持久人物库；
- 群像使用同一规则。

现有 YuNet、SFace 和 face_identity.rs 已有对齐、向量、相似度及一对一分配基础，应扩展复用。

## 3. 五个可选模式与七个最终策略

用户仍只选择自动、风光、人像、环境人像、群像五个拍摄模式；最终评分必须先解析成以下七个策略。清晰门禁只在“确定不清晰”时直接返回 1 星；不确定时继续计算。后续证据按冻结权重形成保守分数区间，缺失项不得当中间分，也不得对已有项重归一化。

| 解析策略          | 第一层清晰门禁 | 门禁后固定权重                                           | 人物规则                                                         |
| ----------------- | -------------- | -------------------------------------------------------- | ---------------------------------------------------------------- |
| 人像              | 人物清晰       | 眼态 40% + 表情 40% + 光学 10% + 构图 10%                | 无指定人物时评估主体；有指定人物时只评估全部确认身份             |
| 群像 + 指定人物   | 指定人物清晰   | 指定人物眼态 40% + 表情 40% + 光学 10% + 构图 10%        | A/B/C 必须一对一且全部确认；疑似/歧义/未知保持 0 星人工复核      |
| 群像 + 无指定人物 | 整图清晰       | 光学 90% + 构图 10%                                      | **忽略所有人脸、人物清晰、眼态和表情证据**                       |
| 环境人像          | 整图清晰       | 光学 50% + 构图 35% + 人物清晰 5% + 眼态 5% + 表情 5%    | 不允许选择指定人物；人物证据只占已确认的 15%                     |
| 风光              | 整图清晰       | 光学 50% + 构图 50%                                      | 忽略远处或背景人物的眼态与表情                                   |
| 自动 + 有人       | 整图清晰       | 人物清晰 50% + 眼态 15% + 表情 15% + 光学 10% + 构图 10% | 不允许选择指定人物；人物状态为加权证据，不是人像模式的眼态硬上限 |
| 自动 + 无人       | 整图清晰       | 光学 50% + 构图 50%                                      | 若仍有较弱人物线索，不得强行当作无人高星，转人工复核             |

眼态上限只适用于人像和“群像 + 指定人物”：单眼闭合、眨眼瞬间、半闭眼或普通闭眼最高 1 星；有意闭目姿态候选最高 3 星。侧脸眼态记 `unknown`；脸部遮挡或看不清属于更严重的人物清晰门禁问题。环境人像和自动有人仅按 5%/15% 加权眼态，不应引入人像式硬上限。

表情只判断技术可用瞬间，不评价情绪、吸引力或“必须微笑”。它只读非眼面部动作证据，不得改写眼态核心。构图模型未放行时必须表达为缺失区间，不得以三分法、居中或对称等手写规则冒充完整审美结论。

## 4. 当前实现差距

| 能力         | 当前状态                                      | 目标差距                                       | 下一动作                               |
| ------------ | --------------------------------------------- | ---------------------------------------------- | -------------------------------------- |
| 配置页 UI    | 已修改并获用户确认                            | 缺桌面自动回归                                 | 只补必要回归                           |
| 模式范围     | 五个可选模式/七策略已建立                     | 缺独立真实任务冻结结论                         | 严格盲测后再校准星级边界               |
| 当前层级扫描 | 已限制为当前文件夹直接图片                    | 缺大目录桌面回归                               | 随 P1 做完整流程验证                   |
| 人工保护     | 显式锁定、持续解锁、复检已完成                | 缺并发与异常恢复冻结回归                       | 建立 R0 冻结用例                       |
| RAW/JPEG     | 默认 JPEG 展示及资产同步已完成                | 分析成员选择尚未通过样片验证                   | 做 RAW/JPEG 成对样片验证               |
| 星级         | 已与组内排名和首选分离                        | 最终分界仍未校准                               | 用真实样片校准曲线                     |
| 自动模式     | 已拆为有人/无人两个固定策略                   | 无可靠人体模型，弱人物线索仍需审核             | 经 `MODEL-01` 和盲测后冻结路由门槛     |
| 环境人像     | 整图清晰门禁 + 50/35/5/5/5 权重               | 光学和构图仍为低置信/缺失证据                  | 模型 Gate 后使用独立任务校准           |
| 关键人物     | A/B/C 一对一证据；仅全 confirmed 才评分       | 自动身份确认仍缺真实标签校准                   | 未确认/缺失保持 0 星人工审核           |
| 表情         | Debug 仅对 confirmed 指定人物产生非眼序列证据 | 无指定人物跟踪、Release/Windows/真实连拍未放行 | 其他情况保留 `unknown` 并完成外部 Gate |
| 风光质量     | 基础清晰/曝光代理 + ARNIQA 隔离 POC           | QRaw 严格运行时失败；构图未生产化              | 运行时、授权、双平台、盲测均通过后接入 |
| 相似组       | 时间、序号、感知哈希                          | 裁剪、姿态和轻微重构图不足                     | 学习特征加严格聚类                     |
| 模型发布     | 仅现有人脸模型                                | 新模型均未获生产放行                           | 隔离 POC，不直接写星                   |
| 效果验证     | 既有样片仅作回归/校准，无新独立盲测集         | 阈值和泛化准确率无法冻结                       | 建立按完整拍摄任务切分的数据集         |

## 5. 技术研究结论与边界

### 5.1 优先复用

- YuNet：继续做人脸检测，补多尺度分块和去重；
- SFace：继续做人物 A/B/C 身份匹配，基于真实照片重新校准；
- 眼态：复用已冻结的 FaceMesh 密集眼睑几何 + Blendshape 眨眼系数交叉证据；其契约为 `qraw-eye-model-contract-1.0` + `qraw-eye-policy-1.0`，表情、光学和构图不得改写；
- 现有 metadata、catalog、asset、scoring 和 API 契约：在原体系扩展；
- ONNX Runtime：继续作为唯一生产推理运行时。

### 5.2 候选能力

| 能力         | 当前优先候选                                 | 用途                         | 状态                                                |
| ------------ | -------------------------------------------- | ---------------------------- | --------------------------------------------------- |
| 人物主体     | RT-DETRv4-S，RT-DETRv2 备选                  | 自动路由、环境人像门槛       | 未接入                                              |
| 通用视觉特征 | DINOv2 ViT-S/14                              | 相似组、构图、自有排序头     | 未接入                                              |
| 无参考画质   | ARNIQA                                       | 技术质量辅助证据             | 隔离导出通过；QRaw 严格运行时失败、未授权、未进生产 |
| 人脸关键点   | MediaPipe FaceMeshV2 ONNX；RTMPose face 备选 | 眼睑、姿态、嘴型、对称、遮挡 | macOS Debug 已接校准适配；Release 不可用            |
| 面部动作     | MediaPipe BlendshapeV2 ONNX                  | 冻结眼态 + 只读非眼表情描述  | macOS Debug 已接校准适配；Release 不可用            |

每个候选必须单独完成：许可与权重再分发审查、参考输出、ONNX 导出、Core ML 和 DirectML 完整图、
数值一致性、真实照片准确率、速度、内存、显存和包体评估。

未经用户批准不得新增依赖。InsightFace 免费预训练模型、MobileCLIP 官方权重等存在商业分发限制，
不得直接打包。MediaPipe 官方 TFLite 只作研发参考；ONNX 转换必须先与官方输出完成逐图数值一致性验证，
不得仅凭社区转换文件进入生产评分，也不引入第二套生产运行时。

FaceMeshV2 + BlendshapeV2 已完成隔离工程 POC：官方 TFLite 契约核对、社区 ONNX
数值对齐、派生图 CPU 零误差、资产 SHA 固定，以及项目内置 ONNX Runtime 1.22 下禁止
CPU 回退的 macOS Core ML 加载与推理。macOS Debug 可使用这些证据运行冻结眼态契约和只读非眼表情序列适配；模型仍不在 Tauri 生产资源中。Release/其他平台必须显式输出 unavailable/`unknown`。仍缺 Windows DirectML、真实连拍标注集、阈值校准与摄影师验收；不得将 Debug POC 结果写成生产放行。

### 5.3 当前算法方案

- 自动路由：人物检测与人脸检测联合判断，灰区双路评分，跨星级则审核；
- 关键人物：多尺度 YuNet、高质量参考、SFace 一对一分配、高/灰/低三区间；
- 眼态：使用已冻结的密集眼睑几何 + 眨眼系数交叉证据；冲突、侧脸或遮挡记 `unknown`，后续功能不得调整该契约；
- 表情：使用只读非眼面部动作证据；只有身份已 confirmed 的指定人物可在同一相似组内比较稳定瞬间。无指定人物不得用“每帧一张脸”冒充身份跟踪；情绪分类置信度不得代替稳定性证据；
- 相似组：时间/序号候选、现有感知哈希、DINOv2 全局和局部特征、灰区几何验证；
- 聚类：高精度优先，锚点或完整连接，禁止链式漂移；
- 序列保护：包围曝光、全景接片、焦点堆栈和有意义的动作变化不得误合并；
- 技术质量：全局缩略图加高分辨率主体局部，多区域清晰、模糊、噪声、曝光和画质证据；
- 构图：DINOv2 特征加 QRaw 真实样片的小型成对排序头，可解释规则只作软证据。

详细候选来源和历史论证见旧需求基线。旧基线若仍描述十模式、递归扫描或排名改变星级，均以本文为准。

## 6. 尚未冻结的问题

这些问题不阻塞安全语义 P0，但阻塞最终自动写星：

- 1 至 5 星的最终分数分界及各模式校准曲线；
- 关键人物明确缺失时固定 1 星，还是最高不超过 2 星；
- 0 星中“未处理”和“人工审核”的最终数据字段与文案；
- RAW/JPEG 配对时技术质量分析 RAW 还是当前展示成员；
- 颜色标签是否继续写入及映射方式；
- 自动模式“可靠人物主题”的高/低/灰区阈值；
- 环境人像人物主题门槛；
- 多关键人物每人允许的参考样本数量和交互；
- 除身份疑似外，哪些跨星级低置信结果进入人工审核；
- 分析、用户确认、最终写入与任务取消的完整生命周期；
- 模型和新增依赖批准；
- macOS/Windows 首发设备矩阵及性能预算；
- 各模式真实照片的准确率、误报率和人工审核率门槛。

以上不能靠主观常数直接冻结，必须用真实摄影样片和双平台结果校准。

## 7. 主要风险

| 编号 | 风险                    | 控制原则                                 | 最低放行条件               |
| ---- | ----------------------- | ---------------------------------------- | -------------------------- |
| R0   | 覆盖人工评分            | 清单检查、写前重检、持续解锁、资产级锁定 | 冻结并发回归中覆盖次数为 0 |
| R1   | 旧文档与新规则冲突      | 本文优先，修改时同步正式文档             | 生产只存在一套有效规则     |
| R2   | 相似组误合并            | 严格聚类、灰区不合并、专项序列保护       | 冻结集错误合并为 0         |
| R3   | 关键人物误认            | 一对一、高/灰/低区间、疑似人工审核       | 已知误认不得自动升星       |
| R4   | 表情/构图主观误判       | 技术可用性、软证据、组内比较、人工兜底   | 未验证证据不得强降星       |
| R5   | 小脸、侧脸、遮挡、背影  | 人物加人脸、多尺度、未知状态             | 无法识别时不得猜测         |
| R6   | RAW/JPEG 结果难解释     | 展示与分析成员明确、成对样片验证         | 资产级结果始终一致         |
| R7   | Core ML/DirectML 不一致 | 固定图、实机任务级对比、禁静默 CPU 回退  | 用户可见离散结果一致       |
| R8   | 模型许可不可分发        | 逐模型审查代码、权重、训练数据           | 许可和第三方通知完备       |
| R9   | 性能和包体失控          | 共享证据、按模式运行、缓存和预算         | 双平台性能预算通过         |
| R10  | 验证集泄漏              | 按完整任务和相似组切分                   | 冻结测试集不参与调参       |
| R11  | 侵入上游                | 业务留在 smart-culling feature           | 全局审查无业务散落         |
| R12  | 覆盖当前暂存 UI         | 开始前查状态，逐行处理重叠文件           | 已验证页面无回退           |

## 8. 下一步实施路线

### P0：安全与数据语义闭环（已完成）

已在不新增模型或依赖的前提下完成：

1. 前后端统一收敛为五模式；
2. 当前目录只扫描直接图片，禁止递归；
3. 在现有 metadata 体系实现显式锁定、持续解锁和写前重检；
4. 完成 RAW/JPEG 资产级保护及默认 JPEG 展示；
5. 分离绝对质量、星级、组内排名和组内首选；
6. 分离普通未评分与 0 星人工审核状态；
7. 后端拒绝非法“模式 + 关键人物”组合；
8. 同步回归测试和真正会指导实现的正式文档。

P0 优先回归用例：

- 同一绝对分在不同组内排名下星级相同；
- 当前目录只处理直接图片，子目录被忽略；
- 用户手动清零仍锁定；
- 持续解锁不会因系统写入重新锁定；
- RAW/JPEG 任一成员人工操作保护整个资产；
- RAW/JPEG 默认展示 JPEG；
- 已删除模式不能进入任务；
- 非法关键人物组合被拒绝；
- 写入前发现并发人工修改时停止覆盖。

### P1：建立保守的五模式 / 七策略骨架

- 移除自动模式的横竖图猜测；
- 先用现有可靠证据建立路由，灰区不强行下结论；
- 建立五个可选模式对应的七个解析策略，冻结清晰门禁与权重；
- 接入关键人物 confirmed、missing、suspected、ambiguous 状态；
- 所有缺失证据保持 unknown；
- 未经验证的自动强降星保持关闭。

### P2：逐模型隔离技术验证

依次验证人物主体、DINOv2、ARNIQA、RTMPose，确有必要再验证表情辅助。每项单独报告复用情况、
授权、ONNX、Core ML、DirectML、准确率、性能、包体及人工兜底；不得直接进入生产写星。

### P3：真实照片校准

建立按完整拍摄任务切分的数据集，覆盖五模式、单人/多人、小脸/侧脸/遮挡/背影、相似人物、
连拍与轻微重构图、包围曝光/全景/焦点堆栈、不同设备和光照、RAW/JPEG 配对。先冻结标注规范，
再调阈值，最终测试集不得参与调参。

### P4：生产放行

只启用通过验证的证据；未达门槛的能力保持人工审核。完成安装包、性能、取消、异常恢复、
双平台和上游隔离回归，并由用户使用真实照片最终确认后，才允许生产自动写星。

## 9. 放行标准

- 范围：生产只剩五模式，只处理同层目录，关键人物范围一致；
- 安全：系统不能锁定，用户锁定和持续解锁语义正确，人工结果覆盖为 0；
- 星级：星级只由绝对质量决定，组内排名和首选完全独立；
- RAW/JPEG：默认显示 JPEG，资产级星级、锁定和审核一致；
- 关键人物：A/B/C 一对一，全部确认后才二次评分，疑似保持 0 星审核；
- 相似组：不跨文件夹、不链式漂移、不误合并特殊序列；
- 模式效果：自动和环境人像路由、人物状态、风光质量达到预先冻结门槛；
- 模型：许可可分发，Core ML/DirectML 用户可见结果一致，无静默 CPU 回退；
- UI：左右上游区域无变化，已验证中部 UI 无回退，快速切图和 RAW/JPEG 不黑屏；
- 隔离：业务实现没有散落到上游代码。

## 10. 新窗口开场指令

> 严格阅读并遵守根目录 AGENTS.md、docs/feature-integration-guidelines.md 和
> docs/智能选图功能逻辑实施交接.md。先检查 Git 暂存状态并保护已有 UI 修改，不新增依赖，
> 不修改上游业务文件。先复核并保护已经完成的 P0 安全语义，再完整阅读 P1 相关代码、调用方和测试，
> 搜索可复用实现后给出短计划。从保守五模式/七策略、自动有人/无人路由、关键人物状态及人工审核分组
> 开始小步实现。未经真实样片、授权及双平台 Gate 验证的模型能力不得参与生产自动写星。

## 11. 一句话交接

配置页中部 UI 与 P0 安全语义已经完成；评分主线已进入五模式/七策略，关键人物仅适用于人像与群像，
随后逐项验证模型、真实样片和双平台效果。

## 12. 2026-08-04 实施结果补充

> 本节是本轮代码完成后的最新状态；若与第 8～11 节的“下一步”描述冲突，以本节和
> `docs/智能选图整改TODO.md` 为准。

### 12.1 已完成

- 最终写入不再依赖“已选池/待选池”，相关移动入口、筛选和数量语义已移除；人工修改的 0 星也会作为人工结果保留并锁定。
- 人工审核改为独立业务状态，与黄色标签彻底解耦；特殊序列保持 0 星人工审核，AI 不写颜色标签。
- 分析完成后只在用户仍处于智能选图页面时自动进入复核；离开页面时不抢占，只通知完成。
- 未确认任务具有安全中断标记、根目录校验、部分取消复核和可见失败；重启不会恢复或泄露人物特征及临时分析结果。
- 打星锁支持无记录 0 星、混合批量状态、持续解锁、图库即时人工保护、RAW/JPEG 资产级保护和逐资产失败结果。
- RAW/JPEG sidecar 使用带恢复日志和补偿的守卫事务，覆盖第二成员失败、进程中断、并发人工修改和部分批量失败。
- 自动、风光、人像、环境人像、群像五个可选模式已解析为七个固定权重策略；缺失证据不再填中间分或重归一化，原因码、置信度、人工审核和星级边界分离。
- 关键人物具备 A/B/C 一对一分配及 `confirmed / missing / suspected / ambiguous / unknown`；只在人像、群像显示，自动、风光和环境人像前后端均拒绝。
- 相似组继续使用锚点阻止链式漂移；可疑包围曝光、焦点堆栈、全景/动作序列进入人工审核，组内排名不改变绝对星级。
- RAW/JPEG 只有拍摄身份一致的唯一 RAW+JPEG 才配对，默认展示 JPEG；歧义、多候选、虚拟副本和成员冲突保守跳过或映射到物理资产。
- 中间区完成弹性布局、可读字号、抽屉/弹窗焦点管理、Esc、遮罩、预览重试与缩略图回退；关键人物预览不再黑屏。
- 修复人物识别完成后被后端默认“自动模式”覆盖的问题；识别后保持用户当前模式和人物选择，不再出现选择面板消失或计数归零的假象。

### 12.2 验证结果

- Rust 智能选图专项：216 通过、0 失败、6 个显式忽略；全 Rust 库：222 通过、0 失败、6 个显式忽略。
- 冻结 FaceMesh/Blendshape 双模型在项目 ONNX Runtime 1.22 + Core ML 严格禁止 CPU 回退下的显式硬件测试：1 通过、0 失败。
- Rust Release `cargo check --lib`：通过；保留既有 Debug/测试辅助接口未使用警告，不把警告伪装为零警告。
- 智能选图前端 ESLint：通过。
- Vite 生产构建：通过。
- Tauri Debug：成功生成 macOS `.app` 与 `.dmg`。
- 真实 macOS：21 张任务分析后自动进入复核；配置页、关键人物图片、复核卡片正常显示，左右上游区域未修改；未执行确认写入，临时任务已放弃。
- 全项目基线仍失败：TypeScript 错误位于既有 `App.tsx`、调整、图库、遮罩等上游/公共文件，智能选图路径无命中；全量 ESLint 为 917 项（864 错误、53 警告），智能选图 scoped ESLint 为 0。
- i18n CI 提取器仍报告既有 `library.viewMode.*` 等上游 locale 缺项；本轮未修改这些上游字典。

### 12.3 仍不得宣称完成

- ARNIQA 虽完成隔离导出和 Python 参考验证，但 QRaw ORT 1.22/Core ML 严格运行时门禁失败；权重/数据授权、Windows DirectML、真实盲测和性能也未放行。构图模型仍未生产化。
- 五个用户模式/七个解析策略的最终阈值、关键人物高/灰/低区间、人物背影/侧脸召回、语义相似组和错误合并为 0 均缺少真实任务冻结集。
- RAW 与 JPEG 谁作为最终技术质量分析成员仍需成对样片；当前默认展示并分析 JPEG。
- Windows、长文件名/双语言、RAW/JPEG 实样、超大任务、性能预算、安装包异常恢复及摄影师验收尚未完成。
- 全项目 `lint`/`typecheck` 仍受既有上游/公共目录错误阻塞；本轮只能声明 feature scoped 检查和生产构建通过。

下一阶段只处理 `docs/智能选图整改TODO.md` 中标记为 `[!]` 的 Gate；在模型许可、真实样片和双平台结果满足前，必须继续使用保守低星或人工审核兜底，不能用未经验证的模型结论自动改写用户评分。
