# AGENTS.md

## 1. 核心原则

- 编码前先思考，不要假设，不要隐藏困惑。
- 先读代码，再写代码。
- 修改必须外科手术式，只改和用户需求直接相关的内容。
- 简单优先，用最少代码解决当前问题。
- 复用优先，不重复实现项目中已经存在的能力。
- 遵循当前代码库惯例，而不是引入个人偏好的新模式。
- 失败要大声说明，不要假装完成。
- 每次修改都必须能解释：为什么改这里，为什么这样改，如何验证它是对的。

## 2. 编码前必须做

在实现之前，必须先完成：

- 阅读相关文件、调用方、导出、测试和已有工具函数。
- 搜索项目中是否已有类似实现。
- 明确任务目标和成功标准。
- 如果需求有多种合理解读，先列出选项并询问，不要暗自选择。
- 如果发现更简单的方案，必须提出。
- 如果发现风险、冲突或不确定性，必须说明。

简单、明确、低风险任务可以直接执行。

涉及以下内容时，必须先说明方案再执行：

- 架构变化
- 新增依赖
- 安全或权限逻辑
- 数据迁移
- 公共 API
- 文件拆分
- 大范围修改
- 可能影响多个模块的行为变化

## 3. 复用优先规则

AI 不应在没有检查现有实现的情况下编写新代码。

在写任何新代码前，必须先自问：

1. 这段代码能不能不写？
2. 这段代码能不能少写？
3. 这段代码能不能复用已有实现？
4. 项目中是否已经有可用的函数、组件、hook、service、schema、类型、配置、常量或工具方法？
5. 附近代码是否已经存在应当遵循的模式？
6. 新增这段代码是否会制造重复逻辑？

执行要求：

- 新增函数前，先搜索同类函数。
- 新增组件前，先搜索同类组件。
- 新增类型前，先搜索已有类型定义。
- 新增常量前，先搜索已有常量或配置。
- 新增校验逻辑前，先搜索已有 schema 或 validator。
- 新增 API 调用前，先搜索已有 client、service 或 repository。
- 新增格式化、转换、解析逻辑前，先搜索已有 helper 或 util。
- 新增错误处理前，先搜索已有错误类型、错误码和错误处理模式。
- 新增状态管理前，先搜索已有 store、context、hook 或状态模式。

搜索应优先覆盖与当前任务相关的目录、命名、调用链、相邻代码和测试。不要为了证明搜索过而无边界扫描整个仓库。

禁止事项：

- 不要复制粘贴已有逻辑后轻微改名。
- 不要因为不了解现有代码而重新实现。
- 不要创建和已有工具函数功能重叠的新函数。
- 不要创建第二套状态管理、请求封装、错误处理或校验模式。
- 不要为了当前任务制造未来维护两套逻辑的成本。

如果确实需要新实现，必须说明：

- 搜索了哪些已有位置。
- 为什么已有实现不能复用。
- 新实现和已有模式如何保持一致。

## 4. 修改范围规则

- 保持改动范围最小。
- 每一行改动都应能追溯到用户需求。
- 匹配现有代码风格，即使你个人会采用不同写法。
- 修复 bug 时，优先添加能复现问题的回归测试。
- 修改行为时，必须添加或更新测试。
- 删除只由本次修改造成的无用导入、变量、函数或文件。
- 多步骤任务必须给出简短计划，并在关键步骤后说明进度和验证结果。

## 5. 禁止事项

- 不要重构无关代码。
- 不要“顺手优化”相邻代码、注释、格式或目录结构。
- 不要添加用户没有要求的功能。
- 不要为一次性逻辑创建通用抽象。
- 不要新增依赖，除非先说明必要性并获得确认。
- 不要读取、打印、提交 `.env`、密钥、token、证书、私钥或其他敏感信息。
- 不要跳过失败测试。
- 不要在未验证时声称完成。
- 不要混合两种架构模式、状态管理模式或代码风格。
- 不要在同一个文件中持续追加逻辑来掩盖设计问题。

如果任务确实需要环境变量或凭据相关信息，只能请求用户提供非敏感的变量名、示例值或脱敏信息，不要直接读取或暴露真实秘密。

## 6. 文件增长控制

AI 编码最常见的问题之一是在同一个文件中不断追加代码，导致文件越来越长、职责越来越混乱。必须主动避免这种情况。

### 6.1 文件增长判断原则

行数阈值只是触发自检的信号，不是唯一判断标准。

比行数更重要的是：

- 文件是否承担多个职责。
- 是否出现重复逻辑。
- 是否难以阅读、测试或复用。
- 是否已经成为所有新逻辑的堆放处。
- 是否可以通过小范围拆分降低复杂度。

简单性不等于把所有代码塞进一个文件。简单性意味着职责清晰、重复更少、调用路径可读、测试容易。

### 6.2 添加代码前的自检

在向已有文件添加代码前，必须检查：

- 这个文件当前的主要职责是什么？
- 新增逻辑是否属于这个职责？
- 新增逻辑是否会引入第二个或第三个职责？
- 是否已有更合适的模块、组件、hook、service、helper、schema 或配置文件？
- 是否应该创建一个小的新文件，而不是继续追加到大文件？

### 6.3 分级触发规则

以下阈值用于触发判断和说明，而不是机械禁止。

- 小于 300 行：可以正常修改，但仍必须保持职责清晰。
- 300 到 500 行：如果新增逻辑会引入新职责，应优先考虑拆分。
- 超过 500 行：新增主要逻辑前，必须进行文件增长自检，并说明为什么继续修改该文件或为什么拆分。
- 超过 700 行：不要继续添加主要业务逻辑。优先创建新模块、子组件、hook、service、helper、schema 或配置文件。
- 超过 800 行：不要让手写源码文件继续增长，除非用户明确要求并说明原因。

### 6.4 单次修改增量规则

- 如果一次修改会向单个文件新增超过 50 行主要逻辑，必须检查是否可以复用或拆分。
- 如果一次修改会向单个文件新增超过 80 行主要逻辑，必须说明是否需要拆分。
- 如果一次修改会向单个文件新增超过 120 行主要逻辑，默认应拆分，除非有明确理由保持在同一文件中。

“主要逻辑”不包括自动生成内容、快照、锁文件、大型静态数据、翻译表、测试夹具或迁移文件。但这些文件也不应被手动塞入无关逻辑。

### 6.5 推荐拆分方式

- UI 子区域拆成子组件。
- 业务逻辑拆到 service 或 helper。
- 复用状态逻辑拆成 hook。
- 常量、schema、配置拆到独立文件。
- 复杂条件判断拆成命名清晰的小函数。
- 测试按行为拆成聚焦的测试文件。

拆分的目的应是隔离职责和降低复杂度，而不是创建通用框架或未来扩展层。

不要借“拆分”为名做大规模无关重构。拆分只应覆盖当前任务直接触碰的逻辑。

### 6.6 反追加规则

- 不要把新的 state、helper、UI、校验、副作用、API 调用都塞进同一个大文件。
- 不要用更多条件分支修补已经混乱的文件。
- 原文件应尽量保持为编排层，而不是所有逻辑的堆放处。
- 如果当前任务会加剧文件膨胀，必须先提出更小的拆分方案。

## 7. 简单性规则

- 用最少的代码解决当前问题。
- 不添加推测性功能。
- 不添加“未来可能会用到”的配置项。
- 不处理当前系统不可能出现的场景。
- 如果实现明显过长，必须重新考虑方案。
- 如果 50 行能解决，不要写 200 行。
- 如果资深工程师会认为这是过度设计，就必须简化。

简单性不等于拒绝拆分。只要拆分能让职责更清楚、重复更少、测试更容易，它就是简单性的体现。

## 8. 验证规则

完成修改前，必须运行与改动相关的检查。

优先使用项目已有命令，不要凭空发明命令。常见命令包括：

- `pnpm lint`
- `pnpm typecheck`
- `pnpm test`
- `pnpm build`
- `npm test`
- `pytest`
- `go test ./...`
- `cargo test`

如果无法运行检查，必须说明：

- 哪个检查没有运行。
- 为什么没有运行。
- 这会带来什么风险。
- 用户可以如何验证。

不要把“没有运行验证”描述成“验证通过”。

## 9. 失败处理

- 如果同一问题连续尝试 3 次仍失败，必须停止盲目尝试。
- 停止后必须总结：已尝试什么、失败原因可能是什么、下一步有哪些选择。
- 不要通过扩大修改范围来掩盖失败。
- 不要删除测试来让结果通过。
- 不要降低验证标准。
- 不要在不理解失败原因时继续堆叠补丁。

## 10. 冲突处理

当规则冲突时，按以下优先级处理：

1. 用户当前明确要求。
2. 当前仓库的项目规则。
3. 已有代码模式和测试。
4. 本文档中的通用规则。
5. 通用最佳实践。

如果仍然冲突，必须暂停并询问。

安全、隐私和凭据保护规则不应被普通实现需求覆盖。如果用户请求可能暴露敏感信息，必须先说明风险并请求脱敏输入。

## 11. 最终回复要求

简单任务可以简短汇报。复杂任务必须完整汇报。

每次完成任务后，至少说明：

- 修改了哪些文件。
- 行为或结果发生了什么变化。
- 运行了哪些验证命令。
- 验证结果是什么。

复杂任务还必须说明：

- 每个文件为什么需要修改。
- 是否复用了已有实现；如果没有，说明原因。
- 是否触发文件拆分或文件增长规则。
- 是否有跳过的检查、剩余风险或需要用户确认的事项。

不要只说“完成了”。
